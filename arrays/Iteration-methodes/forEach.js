[1, 2]
  .forEach((n) => console.log(n))

  //task: Print only even numbers using forEach.
const findEven = [ 1, 2, 3, 4, 5, 6];

findEven.forEach((n) => {
    if (n % 2 == 0) {
      console.log(n);
    }
  });
