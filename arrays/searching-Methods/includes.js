//Checks if array contains a value.
const arr  = [1,2,3,4,5];

const has = arr.includes(10);
console.log(has); // returns only i true or false

// Task:

// Check if 42 exists in ].

const given = [10,20,30,40]

const has42 = given.includes(42);
console.log(has42);