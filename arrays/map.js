const myarr = [1, 2, 3, 4, 5];

const transformarr = myarr.map((n) => n*2)
console.log(transformarr)

//Given [2, 4, 6, 8] return [4, 8, 12, 16] using map().

const taskarr = [2,4,6,8];

const result = taskarr.map((n) => n*2);
console.log(result)