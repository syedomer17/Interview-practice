const arr = [10, 2, 5];

arr.sort((a,b) => a - b);

console.log(arr)

// Task:

// Sort [100, 5, 20, 1] in descending order.

const given = [100, 5, 20, 1]

given.sort((a,b) => b - a)

console.log(given);