const arr = [1,2,3,4,5];

arr.splice(1,2) // it will remove all the emements of array from the index 1 to index 2
console.log(arr)

// Task:

// From [10,20,30,40]
// Remove 20 and insert 25.

const given = [10,20,30,40]

given.splice(1,1,25) // it will remove 1 element from index 1 and insert 25 at index 1

console.log(given);